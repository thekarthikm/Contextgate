# ContextGate runs with zero configuration. Nothing here is required.
#
# Optional: override the HMAC key used to sign the demo session cookie.
# If unset, a documented public demo key is used. There are no real secrets
# in this application — all "sensitive" data is synthetic.
# CONTEXTGATE_SESSION_SECRET=
